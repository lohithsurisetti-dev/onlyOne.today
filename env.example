# =============================================================================
# OnlyOne.Today - Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to git (it's in .gitignore)
# =============================================================================

# -----------------------------------------------------------------------------
# SITE CONFIGURATION
# -----------------------------------------------------------------------------
# The base URL of your site (NO trailing slash)
# Local: http://localhost:3000
# Production: https://onlyonetoday.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Site name for meta tags and branding
NEXT_PUBLIC_SITE_NAME=OnlyOne.Today

# Contact email for support/legal
NEXT_PUBLIC_CONTACT_EMAIL=hello@onlyonetoday.com

# -----------------------------------------------------------------------------
# SUPABASE CONFIGURATION
# -----------------------------------------------------------------------------
# Get these from: https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url_here
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# -----------------------------------------------------------------------------
# OPTIONAL: ANALYTICS & MONITORING
# -----------------------------------------------------------------------------
# Vercel Analytics (automatically enabled if deployed on Vercel)
# No configuration needed - just import the components

# -----------------------------------------------------------------------------
# VERCEL KV (REDIS) - RECOMMENDED
# -----------------------------------------------------------------------------
# Get these from: Vercel Dashboard → Storage → Create KV Database
# Used for:
# - Trending posts cache (5 min TTL) → 500x faster
# - Platform stats cache (1 min TTL) → 100x faster
# - Rate limiting (atomic counters) → 40x faster
# - Rankings/leaderboards → Instant updates
#
# Cost: FREE up to 256 MB storage, 3,000 requests/day
# If not provided, app works fine but slower (no caching)
KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=

# -----------------------------------------------------------------------------
# DEVELOPMENT ONLY
# -----------------------------------------------------------------------------
# Set to 'development' for local dev, 'production' for deployed
NODE_ENV=development

